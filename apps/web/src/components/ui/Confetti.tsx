"use client";

/**
 * Stub replacement for the deleted Confetti animation component.
 * Renders nothing -- the celebration effect is removed.
 */
export function Confetti(_props: { trigger?: number }) {
  return null;
}
