# Что осталось проработать (живой чек-лист)

> Снимок на момент: кейсы (50) и фронт-редизайн приняты и в ветке; база знаний
> переносима (сид-чанки). Детали по панелям — в `REWORK_TZ.md`. Здесь — сжатый
> список «доделать / переделать / удалить» с приоритетами и статусом.

## ✅ Сделано
- **50 кейсов БФЛ** — валидатор + независимая проверка + route-тест (100/100) + состязательная юр-проверка (10/10). Принято.
- **Фронт-редизайн** всех панелей (Центр/История/База знаний/Экзамен/логин и др.) — проверен в браузере. Принято.
- **Переносимость базы знаний РЕШЕНА:** 624 чанка (с embedding) выгружены в репо
  `apps/api/scripts/knowledge_data/legal_chunks.jsonl.gz` + идемпотентный сид
  `scripts/seed_knowledge_chunks.py`. На любом ПК: `uv run python -m scripts.seed_knowledge_chunks` → ровно 624.

## �??? Данные/переносимость (проверить по аналогии с чанками)
- [ ] **Экзамен-вопросы** (`seed_exam_questions.py`) — данные в скрипте? (есть, 200 шт — ок).
- [ ] **Радар** (`seed_legal_updates.py`) — 26 шт в скрипте, переносимо ✅.
- [ ] **Кейсы** — `cases_data/` в репо ✅.
- [ ] Свести единый `scripts/seed_all.py` (кейсы + экзамены + чанки + радар + уровни) — одна команда на новый ПК. **Рекомендация.**

## 🗑 Удалить старый код (отдельный верифицируемый PR — НЕ ломать билд)
> ⚠️ Это хирургия по зависимостям, не `rm`. Подробно — `REWORK_TZ.md §5/§5b`.
- [ ] **PvP** (арена/дуэль/турнир/матчмейкинг): удалить PvP-only файлы.
  - 🔴 **НЕЛЬЗЯ удалять `models/pvp.py` целиком:** `PvPRating` держит лидерборд Базы знаний,
    `PvPDuel`/`PvEBossRun`/`PvELadderRun` — геймификацию и `completion_policy` (TZ-1).
  - Порядок: (1) развязать knowledge-arena лидерборд, (2) отвязать arena/combo-ачивки в
    `gamification.py`, (3) убрать `finalize_pvp_duel` из `completion_policy.py`, и только потом —
    PvP-only сервисы/роуты/фронт. После каждого шага — сборка бэка+фронта зелёная.
  - **Тесты/квиз НЕ трогать** (`app/pvp/quiz/**`, `ws/knowledge.py`, `QuizManyasha`, `KnowledgeBaseBrowser`).
- [ ] **Конструктор** (=`CharacterBuilder` в /training): удалить бэк (`custom_characters.py`,
  `training_presets.py`, модели, `custom_character_stats.py`). Согласовать: фронт-билдер
  остаётся? → тогда нужны бэк-заглушки, иначе 404.
- [ ] **Артефакт `apps/api/scripts/cases_data/_wf_cases_batch.mjs`** — рабочий скрипт оркестрации,
  не нужен в проде. Удалить (безопасно).

## 🟡 Бэкенд-доработки панелей (после фундамента Маняши)
- [ ] **Фундамент Маняши** (§0): сервис-сборщик контекста + эндпоинты (RAG + контекст попытки).
      Разблокирует все 3 панели — делать ПЕРВЫМ.
- [ ] **История**: расширить `/training/history` ветками `case`/`exam`/`pvp_duel`; read-эндпоинты
      разбора (`/quiz/{id}/review`, `/exams/attempt/{id}/review`, `/cases/attempt/{id}/review` —
      данные в БД есть); единый `AttemptDetail` + `/coach`.
- [ ] **База знаний**: убрать AI-помощника → Маняша-эксперт с RAG (новый `POST /knowledge-ai/manyasha`);
      перенести `SYSTEM_PROMPT`/`_build_context_block` из `knowledge_assistant.py` до удаления.
- [ ] **Экзамен**: серверная валидация таймера; один сертификат на user+exam (сейчас копятся);
      возобновление брошенной попытки; убрать N+1 в `list_exams`; `category` в `QuestionResult`.

## 🟢 Мелочи/баги
- [ ] **`learning-path: Unauthorized`** — `GET /learning-path/*` отдаёт 401 в поллинг-петле (видно в
      консоли на всех страницах). Бэкенд-авторизация/контракт — починить (шумит и зря бьёт API).
- [ ] **`/api/chat` (Маняша) без rate-limit и auth** — на проде риск слива бюджета navy.api.
- [ ] **Telegram-ссылка Маняши** — дефолт ведёт на чужого бота (`ailegal_academy_bot`).
- [ ] **Радар**: проверить планировщик `fetch_updates` (подпись «каждые 12 часов» может быть пустой).
- [ ] **`/training`** ещё на старом эмодзи-стиле — добить унификацию (зона фронт-агента).
- [ ] **Экзамен PDF**: «Скачать PDF» = `window.print()` — переименовать в «Печать» или сделать настоящий экспорт.
- [ ] **`cases/[id]/page.tsx`** — если в какой-то ветке всплывёт синтакс-ошибка (`AnimatePresence`),
      это регресс; в моей версии она убрана (фикс дедлока). Свериться при мёрже.

## Порядок (рекомендация)
1. Мёрж текущей ветки (кейсы+фронт+чанки) → чистый `main`.
2. Фундамент Маняши → История → База знаний → Экзамен.
3. Удаление PvP/Конструктора — отдельными верифицируемыми PR (`/code-review ultra`).
4. Сквозная проверка `/verify` + сборка после каждого шага.
